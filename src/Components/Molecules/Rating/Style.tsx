import styled from "styled-components";
import { motion } from "framer-motion";

export const RatingsStyle = styled(motion.div)`
  .rate-number {
    font-style: normal;
    font-size: 1.2rem;
    line-height: 1.4rem;
  }
`;
