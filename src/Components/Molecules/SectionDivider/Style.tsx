import styled from "styled-components";
import { motion } from "framer-motion";

export const SectionDividerStyle = styled(motion.div)`
  border-bottom: 1px solid var(--color-7);
  padding-bottom: 1rem;
`;
